node_modules
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Firebase credentials (NEVER commit these)
firebase-service-account.json
*-firebase-adminsdk-*.json

# Rapports de couverture (artefacts generes par jest --coverage)
coverage/